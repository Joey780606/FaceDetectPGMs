# p07 人臉辨識方法說明

MediaPipe 468點 + 臉部比例特徵 + Z-Score 標準化 k-NN Cosine 比對

---

## 一、整體流程概覽

```
Webcam 影像
    │
    ▼
MediaPipe FaceLandmarker
    │  偵測人臉並輸出 468 個 3D 座標點
    ▼
特徵萃取（face_feature_3d.py）
    │  從 468 點中選 26 個關鍵點，計算 351 維臉部比例特徵向量
    ▼
Z-Score 標準化 + L2 正規化（cosine_matcher_np.py）
    │  消除「人類臉部拓撲相似」的干擾，突顯個人差異
    ▼
k-NN Cosine 相似度比對
    │  與所有訓練樣本比較，取最近 K 個鄰居投票
    ▼
判斷結果：此人 / Unknown
```

---

## 二、特徵萃取（351 維）

### 2.1 為何不用全部 468 點

最初嘗試使用全部 468 個 landmark 的 (x, y, z) 座標，攤平成 1404 維向量。
然而測試發現：**任何兩個人的 Cosine 相似度都在 0.997 以上，完全無法區分**。

原因：MediaPipe 468 點描述的是人類臉部的幾何拓撲結構。由於人臉的結構高度保守
（眼睛在鼻子上方、嘴巴在鼻子下方），1404 維向量的「方向」對所有人類幾乎相同，
Cosine 相似度量測的是向量角度，結果永遠趨近於 1。

### 2.2 改用臉部比例特徵（Facial Anthropometry）

選取 26 個關鍵 Landmark，涵蓋臉部主要區域：

| 區域 | Landmark 索引 | 說明 |
|------|-------------|------|
| 鼻子 | 1, 94, 129, 358 | 鼻尖、鼻基底、左右鼻翼 |
| 眼睛 | 33, 133, 159, 145, 263, 362, 386, 374 | 左右眼四角、上下緣 |
| 眉毛 | 46, 105, 107, 276, 334, 336 | 左右眉外緣、頂點、內緣 |
| 嘴巴 | 61, 291, 13, 14 | 左右嘴角、上下唇中心 |
| 下巴 | 152 | 下巴最低點 |
| 顴骨 | 234, 454 | 左右顴骨 |
| 額頭 | 10 | 額頭中心 |

### 2.3 特徵向量組成（351 維）

**Part A：成對 3D 距離（325 維）**

計算 26 個關鍵點之間所有可能的成對 3D 距離，共 C(26, 2) = 325 對。
每個距離除以 IOD（瞳距）做尺度歸一化，消除臉離鏡頭遠近的影響。

```
距離(點i, 點j) / IOD   → 無論臉大臉小，比值相同
```

這些比值直接編碼臉型特徵，例如：
- 鼻寬 / 瞳距（寬鼻 vs 窄鼻）
- 嘴寬 / 瞳距（大嘴 vs 小嘴）
- 下巴到眼睛距離 / 瞳距（臉長 vs 臉短）
- 顴骨到下巴距離 / 瞳距（臉型輪廓）

**Part B：z 深度（26 維）**

26 個關鍵點各自相對鼻尖的 z 軸深度，除以 IOD。
編碼臉部的立體結構，例如鼻子的高挺程度、眼窩的深淺。

```
z深度(點i) / IOD
```

**最終向量：325 + 26 = 351 維**

### 2.4 IOD 歸一化

IOD（Inter-Ocular Distance，瞳距）= 左眼中心與右眼中心的 3D 距離。

```
左眼中心 = 平均(Landmark 33, 159, 145, 133)
右眼中心 = 平均(Landmark 362, 386, 374, 263)
IOD = 3D 距離(左眼中心, 右眼中心)
```

所有距離與深度值均除以 IOD，確保縮放不變性。
若 IOD < 1e-5（MediaPipe 偵測退化），丟棄此幀。

---

## 三、辨識引擎（Z-Score 標準化 + k-NN Cosine）

### 3.1 為何 Cosine 相似度仍需搭配 Z-Score 標準化

即使改用 351 維比例特徵，直接計算 Cosine 相似度仍然失敗（相似度 ~0.997）。

原因：351 個特徵值都是正數（距離 / IOD），且對所有人類都落在相近的數值範圍，
351 維向量仍然指向「正數象限」的相似方向，角度差異極小。

**解法：Z-Score 標準化**

```
標準化後的特徵 = (原始值 - 所有訓練樣本該維度的平均) / 標準差

意義轉換：
  原本：「我的鼻寬 / 瞳距 = 0.82」         ← 任何人都差不多
  標準化後：「我的鼻寬比訓練樣本平均高 2.3 個標準差」 ← 每個人不同
```

標準化後，向量不再描述「這是一張人臉」，而是描述「這張臉與平均臉的偏差方向」。
不同人的偏差方向不同，Cosine 相似度才真正具有區分能力。

### 3.2 訓練階段

```
1. 收集所有人的所有訓練樣本（100 幀 × N 人）
2. 計算每個特徵維度的全局均值 μ[d] 與標準差 σ[d]
   （標準差為 0 的維度設為 1，代表無辨別力）
3. 對所有樣本做 Z-Score 標準化：
   x'[d] = (x[d] - μ[d]) / σ[d]
4. 對標準化後的向量做 L2 正規化（使長度為 1）
5. 儲存成矩陣（N × 351）備用
```

### 3.3 推論階段（辨識一張臉）

```
1. MediaPipe 取得 468 個 3D 點
2. extractFeatures3D() 萃取 351 維原始特徵向量
3. 用訓練時計算的 μ、σ 做相同的 Z-Score 標準化
4. L2 正規化
5. 矩陣乘法：query 向量 × 訓練樣本矩陣
   → 一次取得與所有 N 個訓練樣本的 Cosine 相似度
6. 取相似度最高的前 K=7 個鄰居
7. 多數決：統計 K 個鄰居各人得票數
8. 獲勝者的平均 Cosine 相似度 ≥ 閾值 → 辨識為此人
   獲勝者的平均 Cosine 相似度 < 閾值 → Unknown（陌生人）
```

### 3.4 Unknown 偵測

不需要事先收集陌生人樣本。
陌生人的標準化偏差向量與任何已訓練者的向量方向都不同，
k-NN 投票後的最高相似度自然會低於閾值。

### 3.5 閾值調整

UI 提供「Cosine 相似度閾值」Slider（範圍 0.10 ~ 0.99，預設 0.75）：

| 閾值方向 | 效果 |
|--------|------|
| 調高（嚴格）| 陌生人更難被誤判為已知人，但已知人也容易被判 Unknown |
| 調低（寬鬆）| 已知人更容易被正確辨識，但陌生人也可能被誤判 |

---

## 四、商用授權安全性

| 元件 | 授權 | 說明 |
|------|------|------|
| MediaPipe FaceLandmarker | Apache 2.0 | Google 提供，可商用 |
| NumPy | BSD | 矩陣運算，可商用 |
| 特徵萃取邏輯 | 自行開發 | 無第三方訓練資料 |
| 辨識模型 | 自行收集樣本訓練 | 無第三方資料集侵權問題 |

整條流程**不使用任何以爬蟲資料集訓練的預訓練模型**（如 ArcFace、FaceNet），
法律風險最低。

---

## 五、主要檔案說明

| 檔案 | 功能 |
|------|------|
| `mp_face_landmarker.py` | MediaPipe FaceLandmarker 封裝，輸出 468 個 3D 點 |
| `face_feature_3d.py` | 特徵萃取：468 點 → 351 維臉部比例向量 |
| `cosine_matcher_np.py` | 辨識引擎：Z-Score 標準化 + k-NN Cosine 比對 |
| `face_recognizer.py` | 整合層：學習、辨識、存檔、載入 |
| `main.py` | CustomTkinter UI |
| `face_model.npz` | 儲存所有訓練樣本（原始特徵向量） |
